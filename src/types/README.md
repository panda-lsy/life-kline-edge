# Types (TypeScript 类型定义)

此目录包含所有 TypeScript 类型定义。

## 类型文件

- `bazi.ts` - 八字相关类型
- `kline.ts` - K线图相关类型
- `api.ts` - API 接口类型
- `common.ts` - 通用类型

## 规范

- 使用 `interface` 定义对象结构
- 使用 `type` 定义联合类型
- 导出所有公共类型
- 添加 JSDoc 注释
