name: CI

on:
  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - main
      - master

jobs:
  # 代码质量检查
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: life-kline-edge/package-lock.json

      - name: Install dependencies
        working-directory: ./life-kline-edge
        run: npm ci

      - name: Run ESLint
        working-directory: ./life-kline-edge
        run: npm run lint || true # 允许失败

  # 类型检查
  type-check:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: life-kline-edge/package-lock.json

      - name: Install dependencies
        working-directory: ./life-kline-edge
        run: npm ci

      - name: TypeScript check
        working-directory: ./life-kline-edge
        run: npx tsc -b --noEmit

  # 构建测试
  build-test:
    name: Build Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: life-kline-edge/package-lock.json

      - name: Install dependencies
        working-directory: ./life-kline-edge
        run: npm ci

      - name: Build
        working-directory: ./life-kline-edge
        run: npm run build
        env:
          NODE_ENV: production

      # 检查构建产物大小
      - name: Check bundle size
        working-directory: ./life-kline-edge
        run: |
          echo "📦 构建产物大小："
          du -sh dist/*
          echo "总大小:"
          du -sh dist
